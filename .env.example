# Web Deploy Helper - 环境变量配置示例
#
# 使用方法:
#   1. 复制此文件为 .env
#   2. 替换下面的值为你自己的配置
#   3. 确保 .env 文件不被提交到版本控制

# ==================== Git 认证 ====================

# HTTPS 认证（推荐）
GIT_TOKEN=your-gitea-personal-access-token
# GIT_USERNAME=your-username  # 可选

# SSH 认证（可选）
# SSH_PRIVATE_KEY=your-ssh-private-key-content
# SSH_KNOWN_HOSTS=ssh-keyscan-output  # 可选

# ==================== 项目特定配置 ====================
# 这些变量可以在 docker-compose.yml 中为特定服务覆盖

# 示例：覆盖项目 B 的配置
# REPO_BRANCH=develop
# BUILD_COMMAND=npm run build:staging

# 前端项目在仓库中的相对路径（如 monorepo 中的子目录）
# REPO_SUBDIRECTORY=frontend

# ==================== 通知配置 ====================
# 错误通知 Webhook URL
# ERROR_WEBHOOK=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=YOUR_KEY